<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /wfh/

    # Handle Front Controller...
    RewriteCond %{REQUEST_URI} !^/wfh/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Block access to sensitive files
<Files .env>
    Order allow,deny
    Deny from all
</Files>
